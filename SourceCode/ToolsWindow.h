#ifndef __TOOLS_WINDOW__#define __TOOLS_WINDOW__#include <vector.h>class ToolsWindow{	public:		enum ToolType { SUPER_SPLINE,			NEW_SPLINE, CHANGE_CONTROL_POINT, ADD_CONTROL_POINT, REMOVE_CONTROL_POINT,			CHANGE_DEGREE,			SURFACE_SELECT,			HAND,			TEXT,			INCREASE_SEG_NUM, DECREASE_SEG_NUM };				ToolsWindow(int windowRsrcId);		~ToolsWindow();				WindowRef GetWindow();		ToolType GetToolType();				void Redraw();				void HandleToolSelection(Point where, bool mouseDown);		void HandleSpaceBarDown();		void HandleSpaceBarUp();		void HandleNumSegsChange(bool numIncreased);				void SetToolType(ToolType tt);				void HandleMouseMoved(Point where);		private:		ToolsWindow();	//Declare away				WindowRef window;		ToolType toolType, lastToolType;		bool toolHelpVisible;		vector<PicHandle> picHs;};#endif